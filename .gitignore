node_modules/
.next/
out/
build/
dist/
.vercel/

.env
.env.local
.env*.local

*.log
.DS_Store
.vscode/
.idea/

next-env.d.ts
*.tsbuildinfo

# Documentacion interna de trabajo (SDDs, work-items, backlog, contexto de proyecto):
# se queda en el disco, fuera del repo.
# El rigor se demuestra donde se lee: en los comentarios de cabecera del codigo,
# en .env.example y en el runbook del README.
doc/*
project-context.md

# Excepcion: spec del API del partner, CITADA por codigo de produccion
# (src/providers/transfi-env.ts, src/providers/payout.ts). Si sale del repo,
# esas referencias quedan colgando.
!doc/transfi-offramp-api-spec.md

# --- endurecimiento .env (2026-08-06) ---
# Se ignora TODO lo que empieza con .env y se rescatan los ejemplos por
# excepcion. El patron anterior enumeraba sufijos peligrosos, y el 2026-08-05
# un `.env.local.bak-<fecha>` con 29 secretos quedo suelto en un repo publico
# porque no TERMINABA en `.local`. Enumerar lo peligroso nunca esta completo.
.env*
!.env.example
!.env*.example
